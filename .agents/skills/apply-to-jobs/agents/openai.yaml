interface:
  display_name: "Apply to Jobs"
  short_description: "Find, submit, and verify job applications"
  default_prompt: "Use $apply-to-jobs to apply to 5 matching roles using my saved profile and handle supported account verification with Gmail and Computer Use."
