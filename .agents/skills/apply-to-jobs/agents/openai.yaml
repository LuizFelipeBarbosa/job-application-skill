interface:
  display_name: "Apply to Jobs"
  short_description: "Build a profile and apply through Chrome"
  default_prompt: "Use $apply-to-jobs to build my candidate profile from uploaded materials, collect only missing directions, find 10 matching jobs primarily on Handshake, and apply through the Chrome extension."
