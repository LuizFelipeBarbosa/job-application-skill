interface:
  display_name: "Apply to Jobs"
  short_description: "Find and submit matching job applications"
  default_prompt: "Use $apply-to-jobs to apply to 5 matching roles using my saved profile, and ask only when the current search or application cannot continue."
