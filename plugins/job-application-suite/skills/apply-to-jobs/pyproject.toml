[project]
name = "job-application-suite-runtime"
version = "0.1.0b1"
description = "Python runtime dependencies for the Job Application Suite skill"
requires-python = ">=3.9,<3.14"
dependencies = [
    "keyring==25.7.0",
]

[tool.uv]
package = false
required-version = ">=0.9.29"
