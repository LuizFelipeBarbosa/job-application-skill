# macOS beta acceptance — 2026-07-20

Status: **partial**. This record contains no candidate data, credentials, clipboard contents, Gmail address, message contents, codes, tokens, or browser-session identifiers.

## Environment

- Source commit: `01a9a3066b935ec8cfe1a7f7c8e3426facc47cba`
- Tester: Codex-assisted local acceptance
- macOS: 26.5.2 (25F84)
- Codex CLI: 0.144.0
- Google Chrome: 150.0.7871.125
- Clean-clone doctor runtime: Python 3.9.6, Node 25.5.0, pnpm 10.28.2, npm 11.8.0
- Acceptance timestamp: 2026-07-20T11:06:40Z

## Results

| Check | Result | Non-sensitive evidence |
| --- | --- | --- |
| Fresh clone and plugin installation | Pass | Cloned public `main` at the source commit, bootstrapped the ignored runtime, installed `job-application-suite@personal` through the clean clone's marketplace, and confirmed plugin version `0.1.0-beta.1`. |
| Doctor | Pass with warnings | Exit `0`; all required checks passed. Warnings were limited to the initially absent `private/` directory and dashboard dependencies before their frozen installs. |
| Plugin, skill, and release validation | Pass | Plugin validator, skill validator, and `scripts/validate_release.py` passed. |
| Python tests | Pass | 30 tests passed. |
| Local dashboard | Pass | Frozen pnpm install, typecheck, lint, 18 unit tests, production build, 10 Playwright tests, and production audit passed with no known vulnerabilities. |
| Hosted dashboard | Pass with development-dependency advisory note | Frozen `npm ci`, lint, build, and 3 privacy/render tests passed. The production-only audit found 0 vulnerabilities; the all-dependency install reported 12 advisories in development-inclusive dependencies. |
| Temporary credential lifecycle | Pass | macOS Keychain backend; a unique synthetic credential was generated without printing the password, copied, cleared by the guarded lease while unchanged, deleted, and confirmed absent. Metadata ended with zero accounts, directory mode `0700`, file mode `0600`, and an empty clipboard. |
| Gmail profile | Pass | Connected-profile operation succeeded without exposing or recording the mailbox address. |
| Gmail synthetic verification message | Pending | The narrow post-request self-message query returned zero candidates. No ordinary mail was searched or read. |
| Chrome and Computer Use diagnostic | Pass | Official Chrome control opened only the local fixture. It filled the synthetic name, uploaded the bundled 77-byte `browser-smoke.txt`, and Computer Use visibly inspected and toggled the synthetic confirmation control. Both interfaces read the final diagnostic pass state; the tab and server were closed. |
| User-authorized application canary | Pending | No bounded application request or final-submission authorization was supplied as part of this acceptance run. |

## Open release gates

- Complete the Gmail synthetic-message selection check.
- Complete one explicit review-before-submit application canary on macOS.
- Complete the independent Windows acceptance record.
- Repair and rerun the failing GitHub Actions workflow before release. The current run fails in Corepack/pnpm setup and in Windows-only POSIX mode assertions.

